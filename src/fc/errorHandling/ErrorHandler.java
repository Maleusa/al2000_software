package fc.errorHandling;
/**
 * Cette classe servira à afficher des erreurs en fonction des codes retourné par
 * les fonctions de AL2000
 * utile pour le debuging?
 * @author yazid
 *
 */
public class ErrorHandler {
public void assertError(int i) {
	switch(i) {
	case 0:
		break;
	default:
		break;
	}
}
}
